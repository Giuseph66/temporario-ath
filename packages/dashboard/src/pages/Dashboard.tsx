export function Dashboard() {
    return (
        <div style={{ padding: 40 }}>
            <div style={{
                fontFamily: "'Fraunces', serif", fontSize: 28,
                color: 'var(--ink-1)', marginBottom: 8,
            }}>
                Dashboard
            </div>
            <div style={{ fontSize: 14, color: 'var(--ink-4)' }}>
                Métricas e resumo do seu agente — em breve.
            </div>
        </div>
    );
}
